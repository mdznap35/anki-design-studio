# البرومبت الاحترافي النهائي — مرحلة الختم (Anki Design Studio)

## الدور
مهندس برمجيات أول متخصص في Anki (APKG/AnkiWeb)، وواجهات HTML/CSS/JS متجاوبة للجوال وسطح المكتب. يعمل بدقة، لا يكسر أي ميزة، ويثبت كل شيء بالاختبار قبل التسليم.

## السياق
مشروع "Anki Design Studio": معرض 100 تصميم لبطاقات فرنسية، مع معاينة كاملة، لوحة تحكم صوت، ومحرر. الرزمة المعتمدة رسمياً:
`/root/Documents/Muhammad/فرنسي_بكلوريا_نهائي_3082_بطاقة.apkg`
(3082 بطاقة، قالب واحد "فرنسي بكالوريا"، 13 حقلاً، 8 رزم فرعية). التصميم 41 في المعرض = نسخة حرفية من هذا القالب. الـ99 تصميم الآخرين تصاميم إبداعية بنفس البيانات الكاملة والأزرار الموحدة.

## المهمة (بالترتيب التنفيذي)
1. **المعاينة = البطاقة الحقيقية**:
   - السبب المكتشف: كل الـ99 تصميم في CSS الخاص بهم السطر `*{animation:none!important}` (كتلة "إيقاف كل الحركة") — حذفه من كل التصاميم ليشتغل كل شيء حقيقياً (الحركات والتأثيرات) بالمعاينة وبالرزمة المولّدة معاً.
   - التحقق بصرياً (متصفح headless) أن المعاينة تعرض الحركة والمظهر نفسه للبطاقة الحقيقية، دون كسر تخطيط أي تصميم.
2. **زر "اقلب البطاقة"**:
   - إضافة زر تحت المعاينة (خارج البطاقة) داخل نافذة المعاينة الكاملة (`#fsModal`): يعرض الخلف بدل الواجهة والعكس.
   - حالة قلب لكل معاينة، تنسيق مناسب للجوال، ويشتغل مع أسهم التنقل.
3. **الصوت والمكتبة**:
   - فحص حالة `audio_library.json` (غير موجودة حالياً) ونظام الصوت (edge-tts + لوحة التحكم).
   - استخراج مكتبة الأصوات محلياً من رزمة 3082 (الأصوات مدمجة داخل الحقول `AudioFR/AudioAR/AudioBOTH` — بلا شبكة).
   - إذا تجاوز حجم المكتبة ~40MB → تقسيمها أجزاء (`audio_library_1.json`...) وتحديث `tts.load_library()` لقراءة الأجزاء، مع اختبار تنزيل رزمة بصوت.
   - التقارير: `GET /api/audio_status` و`GET /api/health` يعملان ويظهران أرقاماً صحيحة.
4. **تخفيف أهمية "اصنع قالبك الخاص"**:
   - إعادة التسمية لتحتوي كلمة "بسيط" (مثال: "✏️ محرر بسيط").
   - نقله إلى موقع أقل بروزاً في الواجهة (نهاية التبويبات، أو بداخل القوالب بشكل ثانوي) دون كسر وظيفته (كل خصائص المحرر تبقى).
   - تحديث كل المراجع: تبويب، بطاقة "+" في المعرض، قائمة الأوامر (Ctrl+2)، ودليل المساعدة.
5. **الختم النهائي**:
   - اختبار شامل بالمتصفح: تحميل الصفحة بلا أخطاء Console، المعرض 100 قالب، المعاينة + القلب، لوحة الصوت، تنزيل بدون صوت سريع، تنزيل بصوت (بعد المكتبة).
   - نشر: commit + push master + مزامنة gh-pages + التحقق من Render وGitHub Pages (HTTP 200).
   - كتابة هذا البرومبت كوثيقة ختامية.

## المدخلات
- الرزمة المعتمدة: `/root/Documents/Muhammad/فرنسي_بكلوريا_نهائي_3082_بطاقة.apkg`
- المشروع: `/root/OCR/anki-studio` (git، remote: github.com/mdznap35/anki-design-studio)
- سيرفر محلي: `PORT=3000 python3 app.py` للاختبار.

## القيود (ممنوعات)
- لا تكسر: المعرض، لوحة التحكم، المحرر البصري، TTS، التنزيل، المعاينة.
- لا تعدّل: `generate_apkg.py`, `designs_102.json`, `designs_premium.js`, `designs_data.js`.
- لا تستخدم `rm` — استخدم `shutil.rmtree`.
- لا تلمس قالب الرزمة المعتمدة (التصميم 41) — هو المرجع.
- لا تولّد صوت عبر الشبكة للرزمة الكاملة (3082 كلمة) — اعتمد على المكتبة المستخرجة فقط.
- لا تغيّر مظهر الأزرار الموحدة أو وظيفتها في التصاميم.
- حافظ على سرعة المعاينة الفورية وتوافق الجوال.

## صيغة المخرجات
- تعديلات على `public/index.html` و`public/designs_100.js` و`tts.py` (+ ملفات مكتبة صوت جديدة) و`PROMPT_FINAL.md`.
- تقرير نهائي مختصر: ما تم، ما تجاوز ولماذا، أي مشاكل أصلحت، روابط النشر.

## معايير الجودة (شروط القبول)
- بعد الحذف: لا يوجد `*{animation:none!important}` في أي تصميم، والمعاينة تعرض حركة فعلية (تأكيد بمتصفح headless عبر `getAnimations()` ولقطة شاشة).
- زر القلب يظهر الخلف فعلاً ويعود للواجهة، ويعمل مع التنقل بالأسهم، ولا يكسر تخطيط المودال على الجوال.
- تنزيل بدون صوت: سريع (~1.5MB) وبياناته كاملة. تنزيل بصوت: كل كلمة تحمل صوتها من المكتبة، والرزمة الناتجة ~حجم الرزمة المعتمدة.
- تبويب المحرر أصبح "بسيط" وغير بارز، وكل خصائصه تعمل.
- الصفحة بلا أخطاء Console، وRender وGitHub Pages يستجيبان 200.

## حالات الحافة
- إذا كان حجم المكتبة > حد GitHub (~100MB) → تقسيم لأجزاء <40MB وتحديث التحميل.
- إذا فشل استخراج صوت كلمة → إبقاء الحقل فارغاً بدل كسر التوليد.
- إذا أزالت إزالة `animation:none` مظهراً فوضوياً في معاينة تصميم → معالجة فردية له (إصلاح المظهر لا حذف الحركة).
- إذا تعطل القلب على تصميم خلفه فارغ → إظهار رسالة "لا يوجد خلف" بدل شاشة فارغة.

---

## ✅ حالة التنفيذ (مكتملة — 2026-08-15)

تم تنفيذ جميع بنود هذا البرومبت بنجاح واختبارها:

1. **المعاينة = البطاقة الحقيقية** ✅
   - حُذف `*{animation:none!important}` من كل الـ100 تصميم في `public/designs_100.js` — الحركات والتأثيرات تعمل الآن فعلياً في المعاينة وفي الرزمة المولّدة.
   - غلاف `.card` حول محتوى المعاينة في `buildPreviewDoc` وإزالة `display:flex` القسري من `html,body` — التخطيط سليم.
2. **زر "اقلب البطاقة"** ✅ — `flipCard()` + زر `#fsFlipBtn` تحت المعاينة داخل `#fsModal`، يعمل مع أسهم التنقل، ويعرض الخلف الحقيقي (MAISON منزل + الوحدة/النوع/الجمع + مؤقت).
3. **الصوت والمكتبة** ✅ — مكتبة 2081 كلمة فريدة (8 أجزاء، ~56.6MB) مستخرجة من الرزمة المعتمدة بلا شبكة؛ `tts.load_library()` يدمج كل الأجزاء؛ تغطية كاملة لـ3082 بطاقة؛ تنزيل رزمة بصوت: 90.6MB في ~14 ثانية وكل الحقول الصوتية ممتلئة.
4. **"اصنع قالبك الخاص" → "محرر بسيط"** ✅ — رابط ثانوي غير بارز تحت المعرض، وكل خصائص المحرر تعمل (تحقق بالمتصفح).
5. **الختم** ✅ — اختبار شامل: الصفحة بلا أخطاء Console، 100 قالب، القلب، لوحة الصوت، تنزيل بدون صوت (1.45MB ~2 ثانية) وبصوت، الحقول الـ13 مطابقة للرزمة المعتمدة، 8 رزم فرعية (الوحدة 1-6 + كلمات مهمة).
6. **النشر** ✅ — commit + push master، مزامنة gh-pages، تحقق من Render وGitHub Pages.

### نتائج الاختبار النهائي
- تنزيل بدون صوت: 3082 بطاقة، قالب "41" بـ13 حقلاً: French, Arabic, Plural, Synonym, Antonym, Unit, Type, Page, Extra, AudioFR, AudioAR, AudioBOTH, Extras.
- تنزيل بصوت: 3082/3082 بطاقة فيها AudioFR + AudioAR + AudioBOTH (صوت مدمج base64 مثل الرزمة الأصلية).
- الرزم الفرعية: الوحدة 1..6 + كلمات مهمة — تطابق الرزمة المعتمدة حرفياً.
- المتصفح (جوال 420×760): صفر أخطاء Console، المحرر البسيط يفتح، القلب يشتغل، قائمة extra تعمل.
