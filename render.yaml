services:
  - type: web
    name: anki-design-studio
    runtime: node
    plan: free
    buildCommand: npm install && pip install genanki
    startCommand: node server.js
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
