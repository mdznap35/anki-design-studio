# البرومبت الاحترافي — المعرض الثاني لرزمة "فرنسي تاسع" + كلمة معاينة حقيقية

## الدور
مهندس واجهات وبيانات خبير (UI/UX + Data Engineer) يعمل على مشروع Anki Design Studio القائم،
بدون كسر أي وظيفة حالية، ويختبر كل شيء في متصفح حقيقي قبل التسليم.

## السياق
- المشروع: `/root/OCR/anki-studio` — `public/index.html` معرض 100 تصميم لبطاقات Anki فرنسية
  + لوحة تحكم صوت + محرر + تنزيل .apkg، بواجهة زجاجية (v6.0) وعربية RTL.
- رزمة جديدة معتمدة: `/root/Documents/Muhammad/الرزم_النظيفة_المفحوصة/فرنسي_تاسع_نظيف.apkg`
  (1475 بطاقة، نموذج واحد "مفردات فرنسي تاسع"، حقول: Français / Arabic / Dossier / AudioFR / AudioAR / AudioBOTH).
- تصميم الرزمة: بطاقة "المشهد" (scene/frame/fitme word/sound) مع 3 أزرار صوت (FR/AR/⇄)
  وصوت تلقائي بعد 520ms، وخلفها يظهر الدوسييه والترجمة مع نفس الأزرار.

## المهمة
1. **كلمة معاينة حقيقية**: استبدال العينة الحالية `SAMP={f:'Maison',a:'منزل',...}` في
   `public/index.html` بكلمة حقيقية من الرزمة: `La scène / مشهد / Dossier 1` مع بياناتها
   الكاملة، وإضافة دعم placeholder `{{Dossier}}` في `fillSampleFields` مع الحفاظ على
   `Pluriel/Synonyme/Contraire` الحالية.
2. **معرض ثانٍ منفصل**: إنشاء صفحة مستقلة `public/gallery-tas9.html` (بذات الهوية الزجاجية
   للنسخة 6.0) تعرض:
   - التصميم الحقيقي للرزمة (qfmt + afmt + css مستخرجة من الـ apkg) داخل iframe معاينة.
   - البطاقة الافتراضية: كلمة "La scène" الحقيقية مع صوتها الحقيقي المدمج (AudioFR/AudioAR).
   - متصفح كلمات: بحث فوري في 1475 كلمة (فرنسي/عربي/دوسييه) + قائمة قابلة للتمرير،
     وعند اختيار كلمة تتحدث المعاينة بها (صوتها عبر TTS المتصفح بصوت رجالي إن لم يكن
     الصوت الحقيقي مدمجاً).
   - زر قلب البطاقة لرؤية الخلف (afmt) + أزرار تنقل/إغلاق.
   - إحصائيات حقيقية (1475 كلمة، عدد الدوسييهات).
   - رابط "→ المعرض الرئيسي" للعودة.
3. **ربط**: إضافة زر/رابط في هيدر الصفحة الرئيسية يفتح المعرض الثاني.

## المدخلات
- `public/index.html` (السكربت الكامل + SAMP) و`public/designs_100.js`.
- مستخرج الرزمة: `/tmp/apkg_tas/` (collection.anki2 → qfmt/afmt/style.css + الكلمات).
- سيرفر الاختبار: `PORT=3000 python3 app.py` على `localhost:3000` (+ نسخة ثابتة لتجربة الصفحة الجديدة).

## القيود (ممنوعات)
- لا تكسر: المعرض الرئيسي، لوحة التحكم، المحرر، TTS، التنزيل، المعاينة، القلب.
- لا تعدّل: `generate_apkg.py`, `designs_102.json`, `designs_premium.js`, `designs_data.js`, `designs_100.js`.
- لا تضيف مكتبات خارجية (لا CDN) — كل شيء مدمج بالصفحة (self-contained).
- الصفحة الجديدة تعمل بدون سيرفر (GitHub Pages) — الأصوات عبر speechSynthesis عند غياب الصوت المدمج.
- متوافقة كلياً مع الجوال (RTL، مقاسات مرنة) وسطح المكتب.
- كود عربي/إنجليزي واضح، بدون أخطاء Console.

## صيغة المخرجات
- `public/gallery-tas9.html` — صفحة واحدة كاملة (CSS+HTML+JS+بيانات مدمجة).
- تعديل `SAMP` + `fillSampleFields` في `public/index.html`.
- رابط في هيدر الصفحة الرئيسية.
- ملف هذا البرومبت `SECOND_GALLERY_PROMPT.md`.

## معايير الجودة (قبول)
1. المعاينة الأولى تعرض "La scène / مشهد / Dossier 1" مطابقة تماماً لتصميم الرزمة الأصلية
   (خلفية المشهد، الخط الملائم، زر الصوت الثلاثي، الصوت الحقيقي يعمل عند الضغط).
2. البحث يشمل 1475 كلمة فورياً ويعرض النتائج (فرنسي + عربي + دوسييه).
3. اختيار أي كلمة يحدّث المعاينة فوراً (نص + صوت عبر TTS بصوت رجالي).
4. زر القلب يعرض الخلف الصحيح (دوسييه + ترجمة + 3 أزرار).
5. `لقطة` Playwright: 0 أخطاء Console على الجوال (420px) وسطح المكتب.
6. الصفحة الرئيسية تفتح المعرض الثاني من الهيدر دون كسر المعرض الحالي.

## حالات الحافة
- كلمات طويلة → نظام fitme يقلّص الخط (مضمّن بتصميم الرزمة).
- متصفح يمنع التشغيل التلقائي → الأزرار تعمل يدوياً دائماً.
- كلمة بلا صوت مدمج (غير الافتراضية) → TTS المتصفح.
- البحث بلا نتائج → رسالة "لا توجد نتائج" + زر مسح.
- فتح المعرض الثاني بدون سيرفر (GitHub Pages) → يعمل بالكامل.
