# البرومبت الاحترافي — معرضان كاملان: بكالوريا + تاسع (نسخة مطابقة) مع إزالة الإضافات والدوسييه

## الدور
مهندس واجهات وبيانات خبير (UI/UX + Data + DevOps) يعمل على مشروع Anki Design Studio القائم،
بدون كسر أي وظيفة حالية، ويختبر كل شيء في متصفح حقيقي قبل التسليم.

## السياق (القرار الحاسم من المستخدم)
- المعرض الرئيسي `public/index.html` = **فرنسي بكالوريا** (3082 كلمة، 100 تصميم) — لا يُمسّ إلا بالتعديلات المطلوبة.
- المعرض الثاني = **نسخة كاملة مطابقة** للمعرض الرئيسي (نفس التبويبات/الواجهة/القوالب/لوحة التحكم/المحرر/النطق/التنزيل)
  منفصلة تماماً لرزمة **فرنسي تاسع** (1475 كلمة) — اسمها `public/gallery-tas9.html` (مربوط في هيدر الرئيسي).
- الاختلافات الوحيدة المصرّح بها بين المعرضين:
  1. رزمة المصدر (تاسع: 1475 كلمة).
  2. كلمة معاينة حقيقية من رزمة كل معرض: الرئيسي = `Un ami/صديق` (من words_final.json)، التاسع = `La scène/مشهد/Dossier 1`.
  3. **حذف قسم الإضافات** (زر ＋ + `div.extra` + دالة `toggleExtra`) من المعرضين.
  4. **حذف رقم الوحدة/الدوسييه** تحت الكلمة من العرض والتنزيل (`kw-sub` / `Dossier X` / `ExtraRows`).
  5. استبدال **قالب الشطرنج** (index 30 في designs_100.js) بقالب احترافي جديد بمستوى الباقي.
- لا تغيير في الأرقام/العدادات من حيث المفهوم: 100 قالب، وعدد كلمات الرزمة لكل معرض (3082 / 1475)، وعدد الأصوات.

## المهمة
1. **إصلاح SAMP الرئيسي**: في `public/index.html` استبدال العينة الحالية بـ `{f:'Un ami',a:'صديق',p:'Des amis',s:'Un copain',c:'Un ennemi',dossier:'الوحدة 1'}`.
2. **إنشاء المعرض الثاني الكامل**: نسخ `public/index.html` إلى `public/gallery-tas9.html` ثم تعديل:
   - `SAMP` → `{f:'La scène',a:'مشهد',p:'',s:'',c:'',dossier:''}` (كلمة حقيقية من رزمة تاسع، بدون عرض الدوسييه).
   - `deckName` → "فرنسي تاسع — نظيف (1475 بطاقة)".
   - عداد الكلمات: 1475، عداد الأصوات: 1475 (كل بطاقة بصوت تلقائي).
   - `wordSource:'tas9'` في كل طلبات `/api/generate` (تنزيل مفرد/متعدد/المحرر) + `?source=tas9` في `audio_status`.
   - نصوص المساعدة/الخطوات: 3082→1475، 2081→1475 (لكل ما يخص رزمة تاسع).
   - رابط هيدر للعودة للمعرض الرئيسي `index.html`.
3. **حذف قسم الإضافات** من القوالب الـ 100 في `public/designs_100.js`:
   - إزالة `<button class="extra-btn"...>` و `<div class="extra" id="extra">...</div>` من الوجه الخلفي.
   - إزالة دالة `function toggleExtra(b){...}` من سكربتات الوجهين.
   - في `fillSampleFields` و`buildCustomCardHTML`: تفريغ `{{ExtraRows}}`/`{{Extras}}`/`{{Dossier}}` بدلاً من "الوحدة: 1...".
4. **استبدال قالب الشطرنج** (index 30، اسمه "31") في `designs_100.js` بقالب احترافي فاخر
   بنفس البنية (scene/fx/frame/fitme word/sound bar + script playFr/playAr/playBoth + autoplay + fitme)
   وبنفس قائمة الحقول، بدون قسم إضافات، مع بقاء العدد 100 والترقيم.
5. **ربط `app.py`**:
   - تحميل `words_tas9.json` (كلمات تاسع: word/arabe/unit من Dossier) عند وجوده.
   - دعم `wordSource:'tas9'` في `/api/generate` و`/api/audio_status?source=tas9` وربطه عبر
     `_settings_hash`/`_should_job`/`_start_job`/`_build_deck_apkg`/`get_all_words`.
   - `extra_rows()` → تُرجع '' (إزالة رقم الوحدة من الحقول المولّدة).
   - إنشاء `words_tas9.json` من بيانات تاسع (بدون صوت — الصوت عبر TTS/الوظائف الخلفية).
6. **اختبار**: Playwright جوال (420px) وسطح مكتب: 0 أخطاء Console، المعاينة/الصوت/التنزيل تعمل،
   القوالب تظهر فوراً، القالب الجديد يعمل.
7. **نشر**: commit + push master → مزامنة gh-pages → تحقق HTTP 200 من الرابطين.

## القيود (ممنوعات)
- لا تكسر: المعرض الرئيسي، لوحة التحكم، المحرر، TTS، التنزيل، المعاينة، القلب، المزامنة.
- لا تضيف ميزات جديدة خارج المطلوب. لا تغيّر تجربة الاستخدام الحالية.
- عند كتابة `</script>` داخل JS استخدم `<\/script>`.
- لا `rm` — استخدم `shutil.rmtree` عند الحاجة.
- متوافقة كلياً مع الجوال وسطح المكتب (RTL، مقاسات مرنة).

## معايير القبول
1. المعرض الرئيسي يعرض "Un ami / صديق" وكلمة المعاينة حقيقية من الرزمة المعتمدة.
2. المعرض الثاني صفحة كاملة مطابقة (نفس التبويبات الست: القوالب/لوحة التحكم/المحرر/النطق/التنزيل/المعاينة الكاملة).
3. لا يوجد زر ＋ إضافات ولا صف "الوحدة/النوع/الجمع" ولا "Dossier X" في أي معاينة أو رزمة مولّدة.
4. قالب الشطرنج اختفى وحل مكانه قالب احترافي فاخر بمستوى باقي القوالب.
5. تنزيل من معرض تاسع يولّد رزمة بكلمات تاسع (1475) باسم "فرنسي تاسع — نظيف (1475 بطاقة)".
6. 0 أخطاء Console على الجوال وسطح المكتب، والقوالب تظهر مباشرة بدون تجميد.
7. الرابطان يعملان HTTP 200 بعد النشر.
