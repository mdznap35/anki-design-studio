# البرومبت الاحترافي — إعادة تصميم واجهة Anki Design Studio (مرحلة الجمال)

## الدور
مصمم ومهندس واجهات أول (UI/UX Engineer) متخصص في تصميمات Linear / Vercel / Apple / Notion / Stripe / Figma،
يعمل داخل مشروع قائم دون كسر أي وظيفة، ويختبر كل شيء قبل التسليم.

## السياق
مشروع "Anki Design Studio" (`/root/OCR/anki-studio` — `public/index.html` + `public/designs_100.js`):
محرر ومعرض 100 تصميم لبطاقات Anki فرنسية، مع لوحة تحكم صوت (مكتبة 2081 كلمة)، محرر بسيط، وتنزيل رزمة .apkg.
الواجهة الحالية وظيفية لكنها قديمة بصرياً (تبويب واحد + شبكة نصوص). المطلوب: إعادة تصميم شاملة
"الأجمل في العالم" مع بقاء كل المعرفات والدوال والوظائف كما هي (لا كسر).

## المهمة (بالتسلسل)
1. **نظام تصميم (Design System) جديد**:
   - خلفية عميقة مع توهجات أورورا متدرجة (بنفسجي/زمردي/وردي) ثابتة وخافتة.
   - سطوح زجاجية (Glassmorphism) للهيدر والمودالات، بظلال عميقة وحدود مضيئة.
   - خطوط: Cairo للعناوين + Inter/Noto للجسم + تحسين التباين.
   - نظام ألوان موحد: بنفسجي #7c3aed → ليلكي #a78bfa → فوشيا، مع أخضر للنجاح.
   - حركات دقيقة: دخول متدرج للبطاقات، hover رفع + توهج، انتقالات 200-300ms، فواصل RTL سليمة.
2. **الهيدر**: زجاجي لاصق، شعار متدرج متوهج، زر إصدار، وأزرار سريعة: دليل (ⓘ) + ثيم (🌙) + أوامر (⌘).
3. **واجهة افتتاحية (Hero)**: عنوان متدرج كبير + وصف + إحصائيات (100 قالب • 3082 كلمة • 2081 صوت) + أزرار
   (استعراض القوالب / جولة سريعة) + بطاقة "كيف يعمل" 3 خطوات.
4. **شريط أدوات المعرض**: بحث + وسوم (tags) + مجموعات (collections) + ترتيب + تبديل عرض — بتصميم زجاجي.
5. **بطاقات المعرض**: معاينة مصغرة حقيقية لكل قالب (iframe مع SRCDOC بتصميم فعلي، تحميل كسول، بدون صوت)،
   رقم القالب، اسمه، مفضلة/تحديد، حركة دخول متدرجة.
6. **شاشة البداية (Onboarding)**: ملء الشاشة أول مرة، تدرج + فقاعات متحركة + خطوات جميلة + نقاط تقدم.
7. **دليل شامل (Help)**: مودال بأقسام: خطوات البدء، لوحة التحكم، الصوت، الاختصارات، الأسئلة الشائعة.
8. **معاينة كاملة (Fullscreen)**: مودال زجاجي، أزرار تنقل أنيقة، زر قلب الخلف، زر "تحديد" و"تنزيل هذا القالب".
9. **تفاصيل صغيرة**: توست زجاجي، شريط حالة أنيق، What's New محدث، حالات فارغة/تحميل جميلة.
10. **النشر**: اختبار متصفح (لا أخطاء Console) → commit + push master → مزامنة gh-pages → تحقق HTTP 200.

## المدخلات
- `public/index.html` (5792 سطراً: CSS + HTML + JS)، `public/designs_100.js` (100 تصميم بكل HTML/CSS).
- سيرفر اختبار: `PORT=3000 python3 app.py` على `localhost:3000`.

## القيود (ممنوعات)
- لا تكسر: المعرض، لوحة التحكم، المحرر البصري، TTS، التنزيل، المعاينة، القلب، المزامنة.
- لا تغيّر أسماء المعرفات أو الدوال الموجودة (tplGrid, fsModal, switchTab, renderGrid, ONBOARD_STEPS ...) —
  التصميم طبقة إضافية فوقها.
- لا تعدّل: `generate_apkg.py`, `designs_102.json`, `designs_premium.js`, `designs_data.js`.
- لا تستخدم `rm` — استخدم `shutil.rmtree`.
- لا تغيّر منطق التصميمات (front/back/css) داخل `designs_100.js`.
- حافظ على سرعة المعاينة الفورية وتوافق الجوال (≤420px) وسطح المكتب.
- لا تضيف ميزات جديدة خارج القائمة.

## صيغة المخرجات
- تعديلات على `public/index.html` (+ `UI_REDESIGN_PROMPT.md`).
- تقرير نهائي: ما نُفذ، ما تُم تجاوزه ولماذا، أي أخطاء أُصلحت، أي تحسينات أداء.

## معايير الجودة
- الصفحة تُفتح بلا أخطاء Console وبلا شبكة بطيئة (الخطوط preload، الثمبنيلات lazy).
- المعرض: 100 بطاقة بمعاينات مصغرة حقيقية تعمل، والبحث/الوسوم/الترتيب تعمل.
- القلب (flip) والمحرر البسيط وتنزيل .apkg (بدون/بصوت) يعملون بعد التعديل تماماً كما قبل.
- شاشة البداية تظهر أول زيارة فقط، والدليل يفتح من الهيدر.
- الوضع الفاتح/الداكن يعملان بجمال متساوٍ.
- كل النصوص عربية سليمة، والمسافات/الزوايا/الظلال موحدة.

## حالات الحافة
- إذا كسر التصميم ميزة → التراجع الفوري عن الجزء المعني فقط (الطبقة بصرية، الوظيفة مقدسة).
- إذا كان الثمبنيل المصدّر ثقيلاً لأي قالب → تجاهله (placeholder جميل) بدل إبطاء الشبكة.
- إذا تعارض الوضع الفاتح مع التوهجات → خفف التوهجات في الوضع الفاتح فقط.
